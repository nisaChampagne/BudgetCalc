export class BudgetItem{
    // or here
    constructor(public description:string, public amount:number){}// dont have to set them here
    // just in the constructor with public next to each name
    // makes them accessible throughout app
}